jenkins:
  systemMessage: "Jenkins configurado automaticamente via JCasC"

credentials:
  system:
    domainCredentials:
      - credentials:
          - usernamePassword:
              scope: GLOBAL
              id: "admin"
              username: "admin"
              password: "admin"
              description: "Credencial de administrador"

authorizationStrategy:
  loggedInUsersCanDoAnything:
    allowAnonymousRead: false
